# Local working files, not for the repo
*.xlsx
*.xls
secrets/
.env
__pycache__/
*.pyc

# Snapshot of the Workbook: contains named individuals. Regenerate locally with
# scripts/extract_baseline.py. See README.
tests/fixtures/baseline.json
